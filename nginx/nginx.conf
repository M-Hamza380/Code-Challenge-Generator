events {}

http {
    server {
        listen 80;

        location / {
            proxy_pass http://frontend:4173;
        }

        location /api/ {
            proxy_pass http://backend:1243/;
            rewrite ^/api(/.*)$ $1 break;
        }
    }
}
